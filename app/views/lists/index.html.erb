<body>
  <h1>Lists</h1>
  <ul>
  <%= @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
  </ul>
  <h5><%= link_to "add new list", new_list_path(@list) %></h5>

</body>
